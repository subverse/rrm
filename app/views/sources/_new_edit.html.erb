<%= render :partial => "shared/titles", :locals => {:tab => "rrm::Quellen (new/edit)", :page => "" }%>

<%form('Neu/Bearbeiten', ["#{imageLinkTo 'Previous_16x16.png', sources_path}"], nil) do %>
  <h2>Quelle</h2>
  <% form_for(@source) do |f| %>
    <%= f.error_messages %>
    <table>
      <tr>
        <td>Name</td>
        <td><%= f.text_field :name %></td>
      </tr>
    </table>
    <%= f.submit 'Bestätigen' %>
  <% end %>
<% end %>
