<% menu do %>
    <%= menu_item "Finder", :filter, "Erweiterte Suche" %>
    <% submenu("Posten") do %>
      <%= submenu_item linkTo "Entremetier", Station.find_by_name("Entremetier") %>
      <%= submenu_item linkTo "Gardemanger", Station.find_by_name("Gardemanger") %>
      <%= submenu_item linkTo "Saucier", Station.find_by_name("Saucier") %>
      <%= submenu_item linkTo "Patessier", Station.find_by_name("Patessier") %>
    <% end %>
    <% submenu_with_top_link("Rezepte", recipes_path) do %>
      <%= submenu_item linkTo("Neues Rezept", new_recipe_path, "", true) if logged_in? %>
    <% end %>
    <% submenu_with_top_link("Zutaten", ingreds_path) do %>
      <%= submenu_item linkTo("Neue Zutat", new_ingred_path) if logged_in? %>
    <% end %>
    <% submenu_with_top_link("Kategorien", ingredtypes_path) do %>
      <%= submenu_item linkTo("Neue Kategorie", new_ingredtype_path) if logged_in? %>
    <% end %>
    <% submenu("sonstiges") do %>
      <%= submenu_item link_to_new_tab("Hilfe", {:controller => 'rrm', :action => 'help'}, "Hilfe") %>  
      <%= submenu_item link_to_new_tab("Listen", lists_path, "Drucken") if logged_in? %>  
      <%= submenu_item linkTo("Quellen", sources_path, "Quellen der Rezepte") %>
      <%= submenu_item linkTo("Einheiten", units_path, "Verwendete Maßeinheiten") %>
      <%= submenu_item linkTo("User", users_path, "Benutzerliste") if logged_in? %>
    <% end %>
    <% if !logged_in? %>
      <%= menu_item_to_new_tab "Login", :login %>
    <% else %>
      <%= menu_item_to_new_tab "logout (#{current_user.login})", :logout %>
    <% end %>
<% end %>
