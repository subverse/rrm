<h3>Zutaten</h3>
  <% if @ingred_list.length > 0 %>
    <br />
    <table>
      <tr>
        <th>Bezeichnung</th>
        <th>Menge</th>
        <th>Einheit</th>
      </tr>
      <% for i in 1..@ingred_list.length %>
        <% item = @ingred_list.shift %>
        <tr>
          <td><%= my_link_to_path "#{item.ingred}", Ingred.find(item.id) %></td>
          <td><%= item.amount %></td>
          <td><%= item.unit %></td>
        </tr>
      <% end %>
    </table>
  <% end %>
